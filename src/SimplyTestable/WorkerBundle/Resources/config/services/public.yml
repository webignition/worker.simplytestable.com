# Services that need to be public to allow them to be retrieved from the container
services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: true

  SimplyTestable\WorkerBundle\Services\Resque\QueueService:

  # For retrieval of commands from container in resque jobs
  SimplyTestable\WorkerBundle\Command\Task\PerformCommand:

  SimplyTestable\WorkerBundle\Command\Task\ReportCompletionCommand:

  SimplyTestable\WorkerBundle\Command\Tasks\RequestCommand:
